# Copy to .env for local publishing (never commit .env).
# Create a token: https://www.npmjs.com/settings/~YOUR_USER/tokens
# Use "Granular access token" with "Publish" for this package, or classic "Automation".
#
# NPM_TOKEN=npm_your_token_here
